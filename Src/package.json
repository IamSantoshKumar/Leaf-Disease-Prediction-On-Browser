{
  "dependencies": {
    "light-server": "^2.9.1",
    "onnxruntime-web": "^1.14.0"
  },
  "devDependencies": {
    "copy-webpack": "^2.0.5",
    "webpack": "^5.80.0",
    "webpack-cli": "^5.0.1"
  },
  "scripts": {
    "build": "npx webpack",
    "serve": "npm run build && npx light-server -s . -p 8080"
  }
}
